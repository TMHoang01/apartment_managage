import 'package:flutter/cupertino.dart';

class GuestAccessFormScreen extends StatelessWidget {
  const GuestAccessFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
